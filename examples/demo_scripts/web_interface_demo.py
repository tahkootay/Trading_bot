#!/usr/bin/env python3
"""
Демонстрация веб-интерфейса торгового бота
Показывает функциональность визуального управления ботом
"""

import webbrowser
import time
import sys
import os

def show_features():
    """Показать функции веб-интерфейса"""
    print("🤖 ВИЗУАЛЬНЫЙ ИНТЕРФЕЙС ДЛЯ УПРАВЛЕНИЯ ТОРГОВЫМ БОТОМ")
    print("=" * 70)
    
    features = [
        ("📊 Дашборд", "Статистика моделей, P&L, точность ML, последнее обучение"),
        ("💾 Управление Данными", "Выбор датасетов для обучения и тестирования"),
        ("🧠 Обучение ML", "Настройка и запуск обучения моделей с прогрессом"),
        ("📈 Бэктестирование", "Тестирование стратегий с детальными настройками"),
        ("🔍 Анализ Сделок", "Подробный анализ каждой сделки с фильтрами"),
        ("⚙️ Модели", "Управление ML моделями и их активация"),
        ("🔧 Настройки", "Конфигурация системы и уведомлений")
    ]
    
    print("\n📋 ОСНОВНЫЕ ФУНКЦИИ:")
    for feature, description in features:
        print(f"  {feature:<20} {description}")
    
    print("\n🎯 КЛЮЧЕВЫЕ ВОЗМОЖНОСТИ:")
    capabilities = [
        "✅ Визуальный выбор данных для обучения (август 10-17, 12 день, 14-17 волатильный)",
        "✅ Интерактивное обучение ML моделей с отображением прогресса",
        "✅ Настраиваемое бэктестирование с различными параметрами",
        "✅ Детальная аналитика по каждой сделке",
        "✅ Мониторинг процессов в реальном времени",
        "✅ Управление множественными ML моделями",
        "✅ Экспорт результатов и отчетов"
    ]
    
    for capability in capabilities:
        print(f"  {capability}")
    
    print("\n💡 КАК ИСПОЛЬЗОВАТЬ:")
    steps = [
        "1. Перейдите к разделу 'Управление Данными' и выберите датасет",
        "2. В разделе 'Обучение ML' настройте параметры и запустите обучение",
        "3. После обучения перейдите к 'Бэктестированию' для тестирования",
        "4. Изучите результаты в разделе 'Анализ Сделок'",
        "5. Управляйте моделями в разделе 'Модели'"
    ]
    
    for step in steps:
        print(f"  {step}")

def open_web_interface():
    """Открыть веб-интерфейс в браузере"""
    url = "http://localhost:8080"
    
    print(f"\n🌐 Открываю веб-интерфейс: {url}")
    print("⏳ Ожидание 3 секунды...")
    time.sleep(3)
    
    try:
        webbrowser.open(url)
        print("✅ Веб-интерфейс открыт в браузере!")
        
        print("\n📱 НАВИГАЦИЯ ПО ИНТЕРФЕЙСУ:")
        print("  • Используйте боковое меню для переключения между разделами")
        print("  • Дашборд показывает текущий статус системы")
        print("  • Все процессы отображаются с прогрессом в реальном времени")
        print("  • Результаты сохраняются автоматически")
        
    except Exception as e:
        print(f"❌ Ошибка при открытии браузера: {e}")
        print(f"📋 Откройте вручную: {url}")

def main():
    """Главная функция демонстрации"""
    show_features()
    
    print("\n" + "=" * 70)
    print("🚀 ВЕБ-ИНТЕРФЕЙС ГОТОВ К ИСПОЛЬЗОВАНИЮ!")
    
    # Проверяем что сервер запущен
    import requests
    try:
        response = requests.get("http://localhost:8080", timeout=2)
        if response.status_code == 200:
            print("✅ Flask API сервер работает на порту 8080")
            
            # Открываем интерфейс
            open_web_interface()
            
            print("\n🔄 СЛЕДУЮЩИЕ ШАГИ:")
            print("  1. Изучите интерфейс в браузере")
            print("  2. Попробуйте запустить обучение ML моделей")
            print("  3. Протестируйте бэктестирование")
            print("  4. Проанализируйте результаты торговли")
            
        else:
            print("❌ Сервер не отвечает")
            
    except requests.RequestException:
        print("❌ Flask сервер не запущен!")
        print("💡 Запустите сервер командой: cd web_interface && python api.py")

if __name__ == "__main__":
    main()